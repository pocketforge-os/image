# The F13 selected persistent owner. Taking the foreground target stops the
# shell; releasing it restores exactly this one owner.
[Unit]
Conflicts=pf-shell-selected.service
After=pf-shell-selected.service
OnSuccess=pf-shell-selected.service
